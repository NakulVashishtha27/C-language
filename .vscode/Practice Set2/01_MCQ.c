#include<stdio.h>
int main() {

    //which of the folling is not valid in C language?
    
    int a=10, b=2;
    int v = 3*3;
    char dt = '21 december 2022';
    
    // The above line is not valid in C language because a char can only hold a single character, not a string or multiple characters.

    return 0;
}
#include <stdio.h>
int main()
{
    float r; // radius
    float π = 22.0/7.0; // pi
    printf("the value of radius\n");
    scanf("%f", &r);
    printf("area of circle is %f", π * r * r);
    return 0;
}
